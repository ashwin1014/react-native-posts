{
  "extends": "@react-native/typescript-config/tsconfig.json",
  "compilerOptions": {
    "baseUrl": ".", // This sets the base directory for the relative paths
    "paths": {
      "src/*": ["src/*"],
    }
  }
}
